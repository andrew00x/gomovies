#!/usr/bin/env bash

title="$1"

if [[ -z "${title}" ]]; then
    echo "Movie title is required"
    exit 1
fi

curl \
  -X GET \
  --data-urlencode "q=${title}" \
  -s \
  -G \
  http://{{ hostvars['gomovies_host'].ansible_host }}:{{ app.config.web_port }}/api/details/search
